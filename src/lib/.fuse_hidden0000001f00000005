export async function sendProfileUpdateEmail(data: {
  to: string;
  name: string;
  changes: string[];
  timestamp: Date;
}) {
  // Implémentation d'envoi d'email
  // Utiliser un service comme SendGrid, Resend, etc.
  console.log('Email de mise à jour profil:', data);
}